#!/usr/bin/env python3
"""Run the poster generator."""

from stp.main import *  # Import everything from main.py

# If main.py has code at module level, it runs automatically
# If main.py has if __name__ == '__main__': that still works