{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug stp (Current Config)",
            "type": "debugpy",
            "request": "launch",
            "module": "stp.main",
            "justMyCode": true,
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}